# Viewer-role API token used server-side to fetch drafts for previews / visual editing.
# Create one with: cd ../studio && npx sanity tokens add "Nuxt previews" --role viewer
SANITY_API_READ_TOKEN=

# Where the standalone Studio runs (used for click-to-edit links).
SANITY_STUDIO_URL=http://localhost:3333
